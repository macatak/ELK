# useful if you don't want to see the actual data but want an indication
# that the something is processing.
# scrolls dots across the string to indicate processing

output
{
  stdout {
    # useful to show progress
    codec => dots
    }
}
